#include <stdio.h>
int main()
{
    int contador=1;
    int numero=1;
    for(contador=5;contador<=10;contador=contador+1) {
        printf("vai aparecer isso ate dar o limite definido\n");
        printf("aeeeeeeeee\n");
    }
    //while (enquanto tal coisa for assim){
    // deve ser feito isso daqui
   // }
   while (contador<=12){
   printf("%i",contador);
   ++contador; }

    //for(defino o valor;define o limite do ciclo,da conta que vou fazer;a operacao que vai ser feita até chegar no limite) {
    // o que deve ser feito, vai aparecer na tela.
    // }


    return 0;
}
